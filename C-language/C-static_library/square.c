#include "main.h"

/**
 * areaOfSquare - function to find area of a square
 * @side: length of side of square
 *
 * Return: 0
 */
int areaOfSquare(int side)
{
	int area;
	area = side * side;
	return (area);
}
