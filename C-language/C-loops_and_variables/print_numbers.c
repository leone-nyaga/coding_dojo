#include <stdio.h>

/**
 * main - entry point
 * description - prints all base 10 numbers
 * Return: (0) success
 */

int main()
{
	int i;
	
	for ( i = 0; i < 10; i++)
	{
		printf("%d\n", i);
	}
	return (0);
}
