#include "main.h"
/**
 * areaOfTriangle - Function to find the area of a triangle
 * @base: the base length
 * @height: the height length
 *
 * Return: 0
 */

int areaOfTriangle(int base, int height)
{
	int area;
	area = 0.5 * base * height;
	return (area);
}
