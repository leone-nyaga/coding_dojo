#include "main.h"

/**
 * areaOfRect - function to calculate area of rectangle
 * @length: length
 * @width: width
 *
 * Return: 0 if success
 */

int areaOfRect(int length, int width)
{
	int area;
	area = length * width;
	return (area);
}
