#ifndef MAIN_H
#define MAIN_h

#include <stdio.h>

#define SIZE 1024
#define PI 3.14159265359
#define ABS(x) ((x) < (0) ? -(x) : (x))
#define SUM(x, y) ((x) + (y))
#define MESSAGE() printf("HELLO WORLD!\n");

#endif
